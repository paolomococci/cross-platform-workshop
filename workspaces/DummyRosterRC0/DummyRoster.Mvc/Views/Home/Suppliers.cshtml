@using DummyRoster.Common.EntityModel.Models
@model IEnumerable<Supplier>

<h2>@ViewData["Title"]</h2>

<table class="table">
  <caption>@ViewData["Title"] registered in the management</caption>
  <thead>
    <tr>
      <th>Id</th>
      <th>Name</th>
      <th>Fondation Date</th>
      <th>Description</th>
      <th>Belonging</th>
    </tr>
  </thead>
  <tbody>
    @if (Model is not null)
    {
      @foreach (Supplier item in Model)
      {
        <tr>
          <td>
            @Html.DisplayFor(
              modelItem => item.Id
            )
          </td>
          <td>
            @Html.DisplayFor(
              modelItem => item.Name
            )
          </td>
          <td>
            @Html.DisplayFor(
              modelItem => item.FoundationDate
            )
          </td>
          <td>
            @Html.DisplayFor(
              modelItem => item.Description
            )
          </td>
          <td>
            @Html.DisplayFor(
              modelItem => item.Belonging
            )
          </td>
        </tr>
      }
    }
  </tbody>
</table>
